# bookmark-shelf

個人用のブックマーク管理アプリです。ビルド不要・インストール不要・サーバー不要で、
HTML ファイルをブラウザで直接開くだけで動作します。タグ・グループによる整理、
検索/並べ替え、ドラッグ&ドロップでの並べ替え、ログイン情報の保存、JSON での
エクスポート/インポートに対応しています。

## このツールでできること

- ブックマークの登録: URL・タイトル・タグ・グループ・ログインID/パスワード・メモを保存
- タグによる絞り込み、タイトル/URL/タグでの検索
- 並べ替え: 手動（ドラッグ&ドロップ）/ タイトル昇順 / タイトル降順
- グループ分け: 複数のブックマークを選択してまとめてグループ化・グループ解除
- ドラッグ&ドロップ: グループ区画間の移動、区画内での並べ替え
- JSON でのエクスポート/インポート（インポート時は不正なデータを自動的に除外し、除外件数を表示）

対応 URL は `http://`、`https://`、`file:///` の3スキームのみです（追加フォームにも
その旨のヒントが表示されます）。

## 2つのバリアント

このツールは同じ見た目・同じ機能を持つ2つの単一 HTML ファイルとして配布されています。
**保存先が異なるだけ**で、それ以外の機能（検索・タグ・グループ・ドラッグ&ドロップ・
エクスポート/インポートなど）はどちらも同じです。どちらか一方だけを使えば十分です。

| | `bookmarks.html` | `bookmarks-filesync.html` |
|---|---|---|
| 保存先 | ブラウザの localStorage（キー `bookmarks_data`） | 自分で選んだ JSON ファイル（ディスク上） |
| 対応ブラウザ | モダンブラウザ全般 | Google Chrome または Microsoft Edge のみ（File System Access API 使用） |
| 複数端末・複数ブラウザでの共有 | 不可（そのブラウザ内に閉じる） | 可能（同じ JSON ファイルを開けば共有できる） |
| 起動時の挙動 | 開けばすぐ使える | 先に JSON ファイルを開く/新規作成する必要がある（次回以降は再接続を提案） |

非対応ブラウザで `bookmarks-filesync.html` を開くと、File System Access API が
無いことを説明する「このブラウザは対応していません」という画面が表示され、
それ以上は操作できません。

### `bookmarks.html`（localStorage版）を選ぶ場合

- とにかく手軽に使いたい
- 1台のブラウザだけで完結すればよい
- Chrome/Edge 以外のブラウザ（Firefox など）を使いたい

### `bookmarks-filesync.html`（File Sync版）を選ぶ場合

- データを自分の管理下にある1つの JSON ファイルとして持ちたい
- 複数のブラウザ・複数の端末で同じファイルを共有したい（ファイルをクラウド同期
  フォルダ等に置く運用は自己責任で行ってください）
- Chrome または Edge を使っている

## 使い方

ビルドやインストールは不要です。ファイルを直接ブラウザで開くだけで動作します。

```
xdg-open bookmarks.html              # localStorage版。任意のモダンブラウザで開ける
xdg-open bookmarks-filesync.html     # File Sync版。Chrome/Edge で開く必要がある
```

`bookmarks-filesync.html` を初めて開いたときは、「ブックマークファイルに接続」
という画面が表示されます。保存先にしたい JSON ファイルを「ファイルを開く」で
選ぶか、「新規作成」で新しい空の JSON ファイルを作成してください。以降の
追加・編集はそのファイルに自動保存されます。次回以降に同じブラウザで開いた
場合は、前回接続していたファイルへの再接続を促す画面が出ます（ブラウザの
権限の仕組み上、再接続には都度クリックでの許可が必要です）。

## 重要: パスワードは平文で保存されます

ログインパスワード欄に入力した値は、**暗号化されずそのまま**保存・表示
されます（アプリ内にも `※ パスワードは平文で保存されます` という警告が
常時表示されます）。これはローカルの個人用ツールとしての意図的な設計判断
であり、実装上の見落としではありません。共有端末や信頼できない環境では
パスワードを保存しないことを推奨します。

- localStorage版: 同じブラウザ・同じプロファイルを使える人なら誰でも
  ブラウザの開発者ツールやローカルストレージを通じて内容を読めます。
- File Sync版: 接続した JSON ファイル自体に平文で書き込まれます。ファイルの
  アクセス権限やバックアップ先の管理に注意してください。

## エクスポート/インポート

ヘッダーの「エクスポート」ボタンで、現在の全ブックマークを
`bookmarks-export-<日時>.json` という名前の JSON ファイルとしてダウンロード
できます。「インポート」ボタンで JSON ファイルを読み込むと、現在のデータを
インポートしたデータで置き換えます（置き換え前に確認ダイアログが出ます）。
インポート時、URL のスキームが不正なブックマークなど検証を通らないエントリ
は自動的に除外され、除外件数が表示されます（無言で消えることはありません）。

File Sync版では、接続中の JSON ファイルへの直接読み書きとは別に、この
エクスポート/インポート機能を手動バックアップ・他ファイルとの入れ替え用
として利用できます（インポートを確定すると接続中のファイルにも書き込まれます）。

## 開発者向け: アーキテクチャ概要

各ファイルは、インライン `<style>` とインライン `<script>` を持つ単一の
IIFE（ES5 スタイルの vanilla JS）で構成される、完結した1ファイルアプリ
です。ビルドツール・パッケージマネージャ・外部依存はありません。

**`bookmarks.html` と `bookmarks-filesync.html` は CSS とレンダリング/UI
ロジックのほぼ全てを重複させています。** 検索・ソート・タグ絞り込み・
ドラッグ&ドロップ・追加/編集フォーム・import のサニタイズ・ログイン情報
表示などの共通挙動を変更する場合は、両方のファイルに同じ変更を適用する
必要があります（共通ソースから生成されているわけではありません）。両者で
意味のある差分があるのは各ファイルの `<script>` 冒頭のデータ層のみです。

各ファイルの script は、おおむね次の順序で構成されています。

1. **データ層**（2ファイルで異なる部分）
   - `bookmarks.html`: `loadData()`/`saveData()` が `localStorage` を同期的に
     読み書きします。ロード時、手動ソート用の `order` フィールドを持たない
     ブックマークは配列インデックスでその場で採番されます（ドラッグ並べ替え
     が実装される前のデータとの互換のための一度きりの移行処理）。
   - `bookmarks-filesync.html`: IndexedDB ベースのストア
     (`idbGetHandle`/`idbSetHandle`/`idbClearHandle`) を持ち、最後に使った
     `FileSystemFileHandle` を記憶して次回起動時に再接続を提案します。
     `boot()` が起動時の接続/再接続/非対応ブラウザのフローを駆動します。
     権限の再付与 (`requestPermission`) にはユーザー操作（クリック）が必須
     のため、保留中のハンドルは IndexedDB から都度取得するのではなく
     `pendingReconnectHandle` に保持されます。書き込みは `saveChain` という
     Promise チェーンを通り、重複する保存（連続したドラッグ&ドロップなど）
     が同じファイルに対して直列化されます。
2. **URL/domain ヘルパー**: `classifyUrl()` が `http://`、`https://`、
   `file:///` のみを許可スキームとします。`deriveDomain()`/
   `deriveTentativeTitle()` はファビコン表示用のドメインやフォールバック
   タイトルを導出します。
3. **`sanitizeBookmark()`**: アプリ内の追加/編集フォームを経由しない
   ブックマークデータ（両ファイルの JSON インポート、File Sync版の初期
   ファイル読込）に対する唯一のバリデーションゲートです。`id` を再導出し、
   各フィールドを期待される型/形状に強制変換し、`classifyUrl()` に一致
   しない URL のエントリを拒否します。
4. **レンダー層**: `render()` が唯一の再レンダリングのエントリポイントで、
   状態変更のたびに呼ばれます。タグナビ・選択バー・グループ区画を文字列
   結合でゼロから再構築します（仮想 DOM や diff はありません）。ブックマーク
   は常に既存の全グループ + 「未分類」区画に並行表示され、「1グループに
   絞り込む」ビューはありません。
5. **ドラッグ&ドロップ**: カードは区画内/区画間の手動並べ替え
   (`moveBookmark`) と、区画の空き領域へのドロップによるそのグループ末尾
   への追加 (`moveToGroupEnd`) の両方に対応します。どちらの経路も事後に
   全ブックマークの `order` を振り直して永続化します。

### データモデル

各ブックマークは次の形をしています。

```
{ id, url, title, tags[], group, loginId, loginPassword, memo, scheme, domain, order }
```

- `group` は文字列または `null`（未分類）。グループは別エンティティではなく、
  複数のブックマークが共有する値に過ぎません。
- `order` は手動ソート用のインデックスで、ソートモードが `manual` のときのみ
  意味を持ちます（`title-asc`/`title-desc` では無視されます）。

### テスト

自動テストスイート・lint・ビルドコマンドは存在しません。変更を検証する
場合は、上記の `xdg-open` コマンドでファイルを開き、手動で機能を確認して
ください。

---
最終更新日: 2026-07-27
